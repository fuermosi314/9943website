# 9943小工具大全 - 工具扩展实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 添加 13 个新工具到网站

**Architecture:** 纯前端实现，每个工具独立页面，使用 pdf-lib 处理 PDF，Canvas API 处理图片

**Tech Stack:** Next.js 14, TypeScript, TailwindCSS, pdf-lib

---

## 准备工作

### Task 0: 安装依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装 pdf-lib**

```bash
cd /home/huang/claude/vs/work/9943小工具大全 && npm install pdf-lib
```

- [ ] **Step 2: 验证安装**

```bash
npm run build
```

---

## 第一批：生活工具

### Task 1: BMI 计算器

**Files:**
- Create: `app/tools/bmi/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建 BMI 计算器页面**

```tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';

export default function BMICalculator() {
  const [height, setHeight] = useState('');
  const [weight, setWeight] = useState('');
  const [unit, setUnit] = useState<'metric' | 'imperial'>('metric');
  const [bmi, setBmi] = useState<number | null>(null);
  const [category, setCategory] = useState('');

  const calculateBMI = () => {
    const h = parseFloat(height);
    const w = parseFloat(weight);
    if (!h || !w) return;

    let bmiValue: number;
    if (unit === 'metric') {
      bmiValue = w / ((h / 100) * (h / 100));
    } else {
      bmiValue = (w / (h * h)) * 703;
    }

    setBmi(parseFloat(bmiValue.toFixed(1)));

    if (bmiValue < 18.5) setCategory('偏瘦');
    else if (bmiValue < 24) setCategory('正常');
    else if (bmiValue < 28) setCategory('偏胖');
    else setCategory('肥胖');
  };

  const getBMIColor = () => {
    if (!bmi) return 'text-white';
    if (bmi < 18.5) return 'text-blue-400';
    if (bmi < 24) return 'text-green-400';
    if (bmi < 28) return 'text-yellow-400';
    return 'text-red-400';
  };

  return (
    <div className="min-h-screen relative z-10">
      <header className="fixed top-0 left-0 right-0 z-50 bg-black/40 backdrop-blur-xl border-b border-white/10">
        <div className="max-w-4xl mx-auto px-6 py-3 flex items-center">
          <Link href="/" className="flex items-center text-white/60 hover:text-[#fb6400] transition-colors mr-6">
            <svg className="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
            </svg>
            返回
          </Link>
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-gradient-to-br from-[#fb6400] to-[#ff8c00] rounded-lg flex items-center justify-center shadow-lg shadow-orange-500/30">
              <span className="text-white text-sm">⚖️</span>
            </div>
            <h1 className="text-lg font-semibold text-white">BMI 计算器</h1>
          </div>
        </div>
      </header>

      <main className="max-w-4xl mx-auto px-6 pt-24 pb-16">
        <div className="animate-fade-in">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div className="glass-card p-6">
              <h2 className="text-base font-semibold text-white mb-4">输入信息</h2>

              <div className="flex space-x-2 mb-4">
                <button
                  onClick={() => setUnit('metric')}
                  className={`flex-1 py-2 rounded-lg text-sm font-medium transition-all ${
                    unit === 'metric'
                      ? 'bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white'
                      : 'bg-white/5 text-white/60 hover:bg-white/10'
                  }`}
                >
                  公制
                </button>
                <button
                  onClick={() => setUnit('imperial')}
                  className={`flex-1 py-2 rounded-lg text-sm font-medium transition-all ${
                    unit === 'imperial'
                      ? 'bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white'
                      : 'bg-white/5 text-white/60 hover:bg-white/10'
                  }`}
                >
                  英制
                </button>
              </div>

              <div className="space-y-4">
                <div>
                  <label className="block text-sm text-white/70 mb-2">
                    身高 ({unit === 'metric' ? '厘米' : '英寸'})
                  </label>
                  <input
                    type="number"
                    value={height}
                    onChange={(e) => setHeight(e.target.value)}
                    placeholder={unit === 'metric' ? '170' : '67'}
                    className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder:text-white/30 focus:outline-none focus:border-[#fb6400]"
                  />
                </div>
                <div>
                  <label className="block text-sm text-white/70 mb-2">
                    体重 ({unit === 'metric' ? '千克' : '磅'})
                  </label>
                  <input
                    type="number"
                    value={weight}
                    onChange={(e) => setWeight(e.target.value)}
                    placeholder={unit === 'metric' ? '65' : '143'}
                    className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder:text-white/30 focus:outline-none focus:border-[#fb6400]"
                  />
                </div>
              </div>

              <button
                onClick={calculateBMI}
                className="w-full mt-4 bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white py-3 rounded-xl hover:shadow-lg hover:shadow-orange-500/30 transition-all font-medium"
              >
                计算 BMI
              </button>
            </div>

            <div className="glass-card p-6">
              <h2 className="text-base font-semibold text-white mb-4">计算结果</h2>

              {bmi ? (
                <div className="animate-fade-in">
                  <div className="text-center mb-6">
                    <div className={`text-6xl font-bold ${getBMIColor()}`}>{bmi}</div>
                    <div className="text-lg text-white/70 mt-2">{category}</div>
                  </div>

                  <div className="space-y-3">
                    <div className="flex justify-between items-center p-3 rounded-lg bg-white/5">
                      <span className="text-white/70">偏瘦</span>
                      <span className="text-blue-400">&lt; 18.5</span>
                    </div>
                    <div className="flex justify-between items-center p-3 rounded-lg bg-white/5">
                      <span className="text-white/70">正常</span>
                      <span className="text-green-400">18.5 - 24</span>
                    </div>
                    <div className="flex justify-between items-center p-3 rounded-lg bg-white/5">
                      <span className="text-white/70">偏胖</span>
                      <span className="text-yellow-400">24 - 28</span>
                    </div>
                    <div className="flex justify-between items-center p-3 rounded-lg bg-white/5">
                      <span className="text-white/70">肥胖</span>
                      <span className="text-red-400">&gt; 28</span>
                    </div>
                  </div>
                </div>
              ) : (
                <div className="flex items-center justify-center h-64 text-white/30">
                  输入信息后点击计算
                </div>
              )}
            </div>
          </div>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 注册工具到 lib/tools.ts**

在 tools 数组中添加：

```typescript
{
  id: 'bmi',
  name: 'BMI 计算器',
  description: '计算身体质量指数，支持公制/英制',
  icon: '⚖️',
  category: 'life',
  path: '/tools/bmi',
  tags: ['bmi', '体重', '健康', '计算'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 2: 随机数生成器

**Files:**
- Create: `app/tools/random-generator/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建随机数生成器页面**

```tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';

export default function RandomGenerator() {
  const [mode, setMode] = useState<'number' | 'string' | 'uuid' | 'password'>('number');
  const [min, setMin] = useState('1');
  const [max, setMax] = useState('100');
  const [length, setLength] = useState('16');
  const [count, setCount] = useState('1');
  const [result, setResult] = useState<string[]>([]);
  const [copied, setCopied] = useState(false);

  const generate = () => {
    const nums = parseInt(count) || 1;
    const results: string[] = [];

    for (let i = 0; i < nums; i++) {
      switch (mode) {
        case 'number': {
          const lo = parseInt(min) || 0;
          const hi = parseInt(max) || 100;
          results.push(String(Math.floor(Math.random() * (hi - lo + 1)) + lo));
          break;
        }
        case 'string': {
          const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
          const len = parseInt(length) || 16;
          let str = '';
          for (let j = 0; j < len; j++) str += chars[Math.floor(Math.random() * chars.length)];
          results.push(str);
          break;
        }
        case 'uuid': {
          results.push(crypto.randomUUID());
          break;
        }
        case 'password': {
          const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*';
          const len = parseInt(length) || 16;
          let str = '';
          for (let j = 0; j < len; j++) str += chars[Math.floor(Math.random() * chars.length)];
          results.push(str);
          break;
        }
      }
    }
    setResult(results);
    setCopied(false);
  };

  const copyToClipboard = () => {
    navigator.clipboard.writeText(result.join('\n'));
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <div className="min-h-screen relative z-10">
      <header className="fixed top-0 left-0 right-0 z-50 bg-black/40 backdrop-blur-xl border-b border-white/10">
        <div className="max-w-4xl mx-auto px-6 py-3 flex items-center">
          <Link href="/" className="flex items-center text-white/60 hover:text-[#fb6400] transition-colors mr-6">
            <svg className="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
            </svg>
            返回
          </Link>
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-gradient-to-br from-[#fb6400] to-[#ff8c00] rounded-lg flex items-center justify-center shadow-lg shadow-orange-500/30">
              <span className="text-white text-sm">🎲</span>
            </div>
            <h1 className="text-lg font-semibold text-white">随机数生成器</h1>
          </div>
        </div>
      </header>

      <main className="max-w-4xl mx-auto px-6 pt-24 pb-16">
        <div className="animate-fade-in">
          <div className="glass-card p-6 mb-6">
            <div className="flex flex-wrap gap-2 mb-6">
              {[
                { id: 'number', label: '随机数' },
                { id: 'string', label: '随机字符串' },
                { id: 'uuid', label: 'UUID' },
                { id: 'password', label: '密码' },
              ].map((item) => (
                <button
                  key={item.id}
                  onClick={() => setMode(item.id as typeof mode)}
                  className={`px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                    mode === item.id
                      ? 'bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white'
                      : 'bg-white/5 text-white/60 hover:bg-white/10'
                  }`}
                >
                  {item.label}
                </button>
              ))}
            </div>

            <div className="space-y-4">
              {mode === 'number' && (
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-sm text-white/70 mb-2">最小值</label>
                    <input type="number" value={min} onChange={(e) => setMin(e.target.value)}
                      className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400]" />
                  </div>
                  <div>
                    <label className="block text-sm text-white/70 mb-2">最大值</label>
                    <input type="number" value={max} onChange={(e) => setMax(e.target.value)}
                      className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400]" />
                  </div>
                </div>
              )}

              {(mode === 'string' || mode === 'password') && (
                <div>
                  <label className="block text-sm text-white/70 mb-2">长度</label>
                  <input type="number" value={length} onChange={(e) => setLength(e.target.value)}
                    className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400]" />
                </div>
              )}

              <div>
                <label className="block text-sm text-white/70 mb-2">生成数量</label>
                <input type="number" value={count} onChange={(e) => setCount(e.target.value)}
                  className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400]" />
              </div>

              <button
                onClick={generate}
                className="w-full bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white py-3 rounded-xl hover:shadow-lg hover:shadow-orange-500/30 transition-all font-medium"
              >
                生成
              </button>
            </div>
          </div>

          {result.length > 0 && (
            <div className="glass-card p-6 animate-slide-up">
              <div className="flex items-center justify-between mb-4">
                <h2 className="text-base font-semibold text-white">结果</h2>
                <button onClick={copyToClipboard} className="text-sm text-[#fb6400] hover:text-[#ff8c00]">
                  {copied ? '已复制 ✓' : '复制全部'}
                </button>
              </div>
              <div className="bg-white/5 rounded-xl p-4 max-h-64 overflow-y-auto">
                {result.map((item, index) => (
                  <div key={index} className="text-white/90 font-mono text-sm py-1 border-b border-white/5 last:border-0">
                    {item}
                  </div>
                ))}
              </div>
            </div>
          )}
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'random-generator',
  name: '随机数生成器',
  description: '生成随机数、字符串、UUID、密码',
  icon: '🎲',
  category: 'life',
  path: '/tools/random-generator',
  tags: ['随机', 'random', 'uuid', '密码', '生成'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 3: 单位换算

**Files:**
- Create: `app/tools/unit-converter/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建单位换算页面**

```tsx
'use client';

import { useState, useEffect } from 'react';
import Link from 'next/link';

const unitCategories = {
  length: {
    name: '长度',
    icon: '📏',
    units: [
      { id: 'm', name: '米', factor: 1 },
      { id: 'km', name: '千米', factor: 1000 },
      { id: 'cm', name: '厘米', factor: 0.01 },
      { id: 'mm', name: '毫米', factor: 0.001 },
      { id: 'mi', name: '英里', factor: 1609.344 },
      { id: 'yd', name: '码', factor: 0.9144 },
      { id: 'ft', name: '英尺', factor: 0.3048 },
      { id: 'in', name: '英寸', factor: 0.0254 },
    ],
  },
  weight: {
    name: '重量',
    icon: '⚖️',
    units: [
      { id: 'kg', name: '千克', factor: 1 },
      { id: 'g', name: '克', factor: 0.001 },
      { id: 'mg', name: '毫克', factor: 0.000001 },
      { id: 't', name: '吨', factor: 1000 },
      { id: 'lb', name: '磅', factor: 0.453592 },
      { id: 'oz', name: '盎司', factor: 0.0283495 },
    ],
  },
  temperature: {
    name: '温度',
    icon: '🌡️',
    units: [
      { id: 'c', name: '摄氏度', factor: 1 },
      { id: 'f', name: '华氏度', factor: 1 },
      { id: 'k', name: '开尔文', factor: 1 },
    ],
  },
  area: {
    name: '面积',
    icon: '📐',
    units: [
      { id: 'm2', name: '平方米', factor: 1 },
      { id: 'km2', name: '平方千米', factor: 1000000 },
      { id: 'ha', name: '公顷', factor: 10000 },
      { id: 'ft2', name: '平方英尺', factor: 0.092903 },
      { id: 'ac', name: '英亩', factor: 4046.86 },
    ],
  },
  volume: {
    name: '体积',
    icon: '🧊',
    units: [
      { id: 'l', name: '升', factor: 1 },
      { id: 'ml', name: '毫升', factor: 0.001 },
      { id: 'm3', name: '立方米', factor: 1000 },
      { id: 'gal', name: '加仑', factor: 3.78541 },
      { id: 'qt', name: '夸脱', factor: 0.946353 },
    ],
  },
  time: {
    name: '时间',
    icon: '⏰',
    units: [
      { id: 's', name: '秒', factor: 1 },
      { id: 'ms', name: '毫秒', factor: 0.001 },
      { id: 'min', name: '分钟', factor: 60 },
      { id: 'h', name: '小时', factor: 3600 },
      { id: 'd', name: '天', factor: 86400 },
      { id: 'w', name: '周', factor: 604800 },
    ],
  },
};

type CategoryKey = keyof typeof unitCategories;

export default function UnitConverter() {
  const [category, setCategory] = useState<CategoryKey>('length');
  const [fromUnit, setFromUnit] = useState('m');
  const [toUnit, setToUnit] = useState('km');
  const [inputValue, setInputValue] = useState('1');
  const [result, setResult] = useState('');

  useEffect(() => {
    const cat = unitCategories[category];
    setFromUnit(cat.units[0].id);
    setToUnit(cat.units[1].id);
  }, [category]);

  useEffect(() => {
    if (!inputValue) {
      setResult('');
      return;
    }

    const cat = unitCategories[category];
    const from = cat.units.find((u) => u.id === fromUnit);
    const to = cat.units.find((u) => u.id === toUnit);
    if (!from || !to) return;

    const value = parseFloat(inputValue);
    if (isNaN(value)) return;

    let converted: number;
    if (category === 'temperature') {
      if (fromUnit === 'c' && toUnit === 'f') converted = value * 9 / 5 + 32;
      else if (fromUnit === 'c' && toUnit === 'k') converted = value + 273.15;
      else if (fromUnit === 'f' && toUnit === 'c') converted = (value - 32) * 5 / 9;
      else if (fromUnit === 'f' && toUnit === 'k') converted = (value - 32) * 5 / 9 + 273.15;
      else if (fromUnit === 'k' && toUnit === 'c') converted = value - 273.15;
      else if (fromUnit === 'k' && toUnit === 'f') converted = (value - 273.15) * 9 / 5 + 32;
      else converted = value;
    } else {
      converted = (value * from.factor) / to.factor;
    }

    setResult(converted % 1 === 0 ? String(converted) : converted.toFixed(6).replace(/\.?0+$/, ''));
  }, [inputValue, fromUnit, toUnit, category]);

  const swapUnits = () => {
    setFromUnit(toUnit);
    setToUnit(fromUnit);
  };

  return (
    <div className="min-h-screen relative z-10">
      <header className="fixed top-0 left-0 right-0 z-50 bg-black/40 backdrop-blur-xl border-b border-white/10">
        <div className="max-w-4xl mx-auto px-6 py-3 flex items-center">
          <Link href="/" className="flex items-center text-white/60 hover:text-[#fb6400] transition-colors mr-6">
            <svg className="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
            </svg>
            返回
          </Link>
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-gradient-to-br from-[#fb6400] to-[#ff8c00] rounded-lg flex items-center justify-center shadow-lg shadow-orange-500/30">
              <span className="text-white text-sm">📏</span>
            </div>
            <h1 className="text-lg font-semibold text-white">单位换算</h1>
          </div>
        </div>
      </header>

      <main className="max-w-4xl mx-auto px-6 pt-24 pb-16">
        <div className="animate-fade-in">
          <div className="glass-card p-6 mb-6">
            <div className="flex flex-wrap gap-2 mb-6">
              {(Object.keys(unitCategories) as CategoryKey[]).map((key) => (
                <button
                  key={key}
                  onClick={() => setCategory(key)}
                  className={`px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                    category === key
                      ? 'bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white'
                      : 'bg-white/5 text-white/60 hover:bg-white/10'
                  }`}
                >
                  {unitCategories[key].icon} {unitCategories[key].name}
                </button>
              ))}
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-4 items-end">
              <div>
                <label className="block text-sm text-white/70 mb-2">从</label>
                <select
                  value={fromUnit}
                  onChange={(e) => setFromUnit(e.target.value)}
                  className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400] mb-2"
                >
                  {unitCategories[category].units.map((u) => (
                    <option key={u.id} value={u.id} className="bg-gray-900">{u.name}</option>
                  ))}
                </select>
                <input
                  type="number"
                  value={inputValue}
                  onChange={(e) => setInputValue(e.target.value)}
                  className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400]"
                />
              </div>

              <div className="flex justify-center">
                <button onClick={swapUnits} className="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center hover:bg-white/20 transition-colors">
                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M7 16V4m0 0L3 8m4-4l4 4m6 0v12m0 0l4-4m-4 4l-4-4" />
                  </svg>
                </button>
              </div>

              <div>
                <label className="block text-sm text-white/70 mb-2">到</label>
                <select
                  value={toUnit}
                  onChange={(e) => setToUnit(e.target.value)}
                  className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:border-[#fb6400] mb-2"
                >
                  {unitCategories[category].units.map((u) => (
                    <option key={u.id} value={u.id} className="bg-gray-900">{u.name}</option>
                  ))}
                </select>
                <div className="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white min-h-[48px]">
                  {result || '结果'}
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'unit-converter',
  name: '单位换算',
  description: '长度、重量、温度、面积、体积、时间单位互转',
  icon: '📏',
  category: 'life',
  path: '/tools/unit-converter',
  tags: ['单位', '换算', 'unit', 'convert'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 4: 专业计算器

**Files:**
- Create: `app/tools/calculator/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建专业计算器页面**

由于计算器代码较长，使用 Agent 创建：

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'calculator',
  name: '专业计算器',
  description: '标准/科学/程序员/日期四种模式',
  icon: '🧮',
  category: 'life',
  path: '/tools/calculator',
  tags: ['计算器', 'calculator', '科学', '程序员', '进制'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

## 第二批：图片工具

### Task 5: 图片格式转换

**Files:**
- Create: `app/tools/image-convert/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建图片格式转换页面**

```tsx
'use client';

import { useState, useRef } from 'react';
import Link from 'next/link';

const formats = [
  { id: 'image/png', name: 'PNG', ext: '.png' },
  { id: 'image/jpeg', name: 'JPG', ext: '.jpg' },
  { id: 'image/webp', name: 'WebP', ext: '.webp' },
  { id: 'image/bmp', name: 'BMP', ext: '.bmp' },
];

export default function ImageConvert() {
  const [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState('');
  const [targetFormat, setTargetFormat] = useState('image/png');
  const [quality, setQuality] = useState(90);
  const [isDragging, setIsDragging] = useState(false);
  const fileInputRef = useRef<HTMLInputElement>(null);

  const handleFile = (selectedFile: File) => {
    if (!selectedFile.type.startsWith('image/')) return;
    setFile(selectedFile);
    const reader = new FileReader();
    reader.onload = (e) => setPreview(e.target?.result as string);
    reader.readAsDataURL(selectedFile);
  };

  const handleConvert = () => {
    if (!preview || !file) return;

    const img = new Image();
    img.onload = () => {
      const canvas = document.createElement('canvas');
      canvas.width = img.width;
      canvas.height = img.height;
      const ctx = canvas.getContext('2d');
      if (!ctx) return;

      ctx.drawImage(img, 0, 0);
      const dataUrl = canvas.toDataURL(targetFormat, quality / 100);

      const link = document.createElement('a');
      const ext = formats.find((f) => f.id === targetFormat)?.ext || '.png';
      link.download = file.name.replace(/\.[^.]+$/, ext);
      link.href = dataUrl;
      link.click();
    };
    img.src = preview;
  };

  return (
    <div className="min-h-screen relative z-10">
      <header className="fixed top-0 left-0 right-0 z-50 bg-black/40 backdrop-blur-xl border-b border-white/10">
        <div className="max-w-4xl mx-auto px-6 py-3 flex items-center">
          <Link href="/" className="flex items-center text-white/60 hover:text-[#fb6400] transition-colors mr-6">
            <svg className="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 19l-7-7 7-7" />
            </svg>
            返回
          </Link>
          <div className="flex items-center space-x-3">
            <div className="w-8 h-8 bg-gradient-to-br from-[#fb6400] to-[#ff8c00] rounded-lg flex items-center justify-center shadow-lg shadow-orange-500/30">
              <span className="text-white text-sm">🔄</span>
            </div>
            <h1 className="text-lg font-semibold text-white">图片格式转换</h1>
          </div>
        </div>
      </header>

      <main className="max-w-4xl mx-auto px-6 pt-24 pb-16">
        <div className="animate-fade-in">
          <div
            className={`glass-card p-8 cursor-pointer transition-all duration-300 ${isDragging ? 'border-[#fb6400] bg-[#fb6400]/10' : ''}`}
            onClick={() => fileInputRef.current?.click()}
            onDragOver={(e) => { e.preventDefault(); setIsDragging(true); }}
            onDragLeave={() => setIsDragging(false)}
            onDrop={(e) => { e.preventDefault(); setIsDragging(false); handleFile(e.dataTransfer.files[0]); }}
          >
            <input ref={fileInputRef} type="file" accept="image/*" onChange={(e) => e.target.files?.[0] && handleFile(e.target.files[0])} className="hidden" />
            {preview ? (
              <img src={preview} alt="预览" className="max-h-48 mx-auto rounded-xl" />
            ) : (
              <div className="py-12 text-center text-white/40">
                <p>点击或拖拽上传图片</p>
              </div>
            )}
          </div>

          {file && (
            <div className="mt-6 glass-card p-6 animate-slide-up">
              <div className="mb-4">
                <span className="text-sm text-white/70">原始格式: {file.type}</span>
              </div>

              <div className="mb-4">
                <label className="block text-sm text-white/70 mb-2">目标格式</label>
                <div className="flex flex-wrap gap-2">
                  {formats.map((f) => (
                    <button
                      key={f.id}
                      onClick={() => setTargetFormat(f.id)}
                      className={`px-4 py-2 rounded-lg text-sm font-medium transition-all ${
                        targetFormat === f.id
                          ? 'bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white'
                          : 'bg-white/5 text-white/60 hover:bg-white/10'
                      }`}
                    >
                      {f.name}
                    </button>
                  ))}
                </div>
              </div>

              {targetFormat === 'image/jpeg' || targetFormat === 'image/webp' ? (
                <div className="mb-4">
                  <label className="block text-sm text-white/70 mb-2">质量: {quality}%</label>
                  <input type="range" min="10" max="100" value={quality} onChange={(e) => setQuality(Number(e.target.value))}
                    className="w-full h-2 bg-white/10 rounded-full appearance-none cursor-pointer accent-[#fb6400]" />
                </div>
              ) : null}

              <button
                onClick={handleConvert}
                className="w-full bg-gradient-to-r from-[#fb6400] to-[#ff8c00] text-white py-3 rounded-xl hover:shadow-lg hover:shadow-orange-500/30 transition-all font-medium"
              >
                转换并下载
              </button>
            </div>
          )}
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'image-convert',
  name: '图片格式转换',
  description: 'JPG/PNG/WebP/BMP 格式互转',
  icon: '🔄',
  category: 'image',
  path: '/tools/image-convert',
  tags: ['图片', '格式', '转换', 'png', 'jpg', 'webp'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 6: 图片裁剪

**Files:**
- Create: `app/tools/image-crop/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建图片裁剪页面**

使用 Agent 创建（代码较长）

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'image-crop',
  name: '图片裁剪',
  description: '自定义区域裁剪图片',
  icon: '✂️',
  category: 'image',
  path: '/tools/image-crop',
  tags: ['图片', '裁剪', 'crop'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 7: 图片调整大小

**Files:**
- Create: `app/tools/image-resize/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建图片调整大小页面**

使用 Agent 创建

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'image-resize',
  name: '图片调整大小',
  description: '按比例或像素调整图片尺寸',
  icon: '📐',
  category: 'image',
  path: '/tools/image-resize',
  tags: ['图片', '调整', '大小', 'resize', '缩放'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

### Task 8: 图片旋转/翻转

**Files:**
- Create: `app/tools/image-rotate/page.tsx`
- Modify: `lib/tools.ts`

- [ ] **Step 1: 创建图片旋转/翻转页面**

使用 Agent 创建

- [ ] **Step 2: 注册工具到 lib/tools.ts**

```typescript
{
  id: 'image-rotate',
  name: '图片旋转/翻转',
  description: '旋转角度、水平/垂直翻转',
  icon: '🔃',
  category: 'image',
  path: '/tools/image-rotate',
  tags: ['图片', '旋转', '翻转', 'rotate', 'flip'],
},
```

- [ ] **Step 3: 验证**

```bash
npm run build
```

---

## 第三批：PDF 工具

### Task 9-13: PDF 工具

每个 PDF 工具使用 Agent 创建，注册到 lib/tools.ts

---

## 最终验证

- [ ] **Step 1: 运行构建**

```bash
npm run build
```

- [ ] **Step 2: 启动开发服务器**

```bash
npm run dev
```

- [ ] **Step 3: 测试所有工具页面**

访问 http://localhost:3000 测试所有新工具

---

**Plan Status:** Ready for execution
**Last Updated:** 2026-05-24
